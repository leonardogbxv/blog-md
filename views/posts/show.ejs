<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" integrity="sha384-JcKb8q3iqJ61gNV9KGb8thSsNjpSL0n8PARn9HuZOnIxN0hoP+VmmDGMN5t9UJ0Z" crossorigin="anonymous" />
  <link rel="stylesheet" href="/stylesheets/css/custom.css" type="text/css" />
  <title>Blog MD</title>
</head>
<body class="bg-lighter">
  <%- include('../partials/_header') %> 
  
  <div class="container mw-6 p-5 align-middle bg-light">

    <h3 class="font-weight-bold mb-1"><%= post.title %></h3>
    <div class="text-muted mb-2"><%= formatDate(post.createdAt) %></div>
    <a href="/" class="btn btn btn-secondary mb-3">Todos os Posts</a>

    <% if(userId) { %> 
      <a href="/posts/edit/<%= post.id %>" class="btn btn-info mb-3">Edit</a>
    <% } %>
    
    <% if(post.image) { %> 
      <div class="">
        <img src="../uploads/images/<%= post.image %>" alt="blog-img" class="img-fluid rounded mb-3" />
      </div>
    <% } %>
    
    <div class="min-vh-100">
      <%- post.sanitizedHTML %> 
    </div>
    
  </div>
  
  <%- include('../partials/_footer.ejs') %>
</body>
</html> 